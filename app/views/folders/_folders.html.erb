<% folders.each do |folder|%>
  <%= render partial: "folders/folder", locals: { folder: folder } %>
<% end %>
<% notes.each do |note|%>
  <%= render partial: "notes/note", locals: { note: note } %>
<% end %>